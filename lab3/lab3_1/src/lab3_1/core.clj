(ns lab3-1.core
  (:gen-class))

(defn filter-future [pred coll]
  
  )

(defn my-partition [n coll] ;каждый кусок из n элементов
  ;take and drop
  )


(defn -main
  [& args]
  (println "Hello, World!"))
